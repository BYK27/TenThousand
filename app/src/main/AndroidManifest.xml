<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Od Androida 13 notifikacije traže runtime dozvolu; traži je MainActivity. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--
        Za focus tajmer ne postoji odgovarajući foregroundServiceType:
        shortService ima limit od 3 minuta, mediaPlayback/location/dataSync se
        ne odnose na ovo. specialUse je jedini pošten izbor. Ako aplikacija
        ikad ide na Play Store, ovaj tip traži pisano obrazloženje pri review-u.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
        USE_EXACT_ALARM (API 33+) se dodeljuje automatski, bez pitanja korisnika,
        aplikacijama čija je osnovna funkcija alarm ili tajmer. Potreban je jer
        coroutine delay() ne budi CPU iz doze režima - bez egzaktnog alarma bi
        tajmer sa ugašenim ekranom istekao sa zakašnjenjem.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".TenThousandApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/aloofly_honest"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/aloofly_honest_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TenThousand">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.TenThousand">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.FocusService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="focus_session_timer" />
        </service>
    </application>

</manifest>